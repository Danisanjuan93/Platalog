FOS\UserBundle\Model\User:
    exclusion_ policy: all
    xml_root_name: users
    properties:
        username:
            groups: [api]
            type: string
        email:
            groups: [api]
            type: string
        enabled:
            groups: [ List ]
            type: boolean
